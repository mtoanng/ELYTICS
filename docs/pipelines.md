a general note to self

Pipelines:

for PR into develop, changelog must be updated - if there's no change, PR is auto denied (PR check basically)
for PR into main, changelog must not contain any WIP versions - if there's WIP, PR is auto denied